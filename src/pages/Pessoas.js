import React, { useContext } from "react";
import { View, Text } from "react-native";
import { MainContext } from "../context/MainContext";

export default function Pessoas(){
    return(
        <View>
            <Text>Página pessoas</Text>
        </View>
    );
}