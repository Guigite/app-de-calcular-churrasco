import React, { useContext } from "react";
import { View, Text } from "react-native";
// import { MainContext } from "../context/MainContext";

export default function Pessoas(){
    return(
        <View>
            <Text>Pagina pessoas</Text>
        </View>
    );
}